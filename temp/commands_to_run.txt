For validate_spec.py:

cd C:\path\to\NLPipeline\scripts
py validate_spec.py ..\temp\spec.json caps.json

Should output "OK" or any errors.


For gen_uml_from_spec.py:
py gen_uml_from_spec.py ..\temp\spec.json template.uml.tpl ..\temp

#Should output a .uml file

For n8n webhook pipeline:

$body = @{
    requirement = "If someone arrives, turn the lamp on. When they leave, turn it off."
    bundle_name = "Bundle1"
    } | ConvertTo-Json
Invoke-RestMethod -Method POST `
    -Uri "http://localhost:5678/webhook-test/NLPipeline/extract" `
    -ContentType "application/json" `
    -Body $body